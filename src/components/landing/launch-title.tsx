export function LaunchTitle() {
  return (
    <h1 className="launch-title" aria-label="正在构建未来">
      <span aria-hidden="true" className="launch-title__build">
        正在构建
      </span>
      <span aria-hidden="true" className="launch-title__future">
        未来
      </span>
    </h1>
  );
}
