# 18 · 需求变更记录 v1.1（算力市场扩展）

> 版本：v1.1 ｜ 日期：2026-07-29 ｜ 状态：实施中
> 来源：业务负责人 2026-07-29 提出的算力市场扩展需求
> 影响范围：算力撮合模块（docs/03）、居间金融模块（docs/07）、供给方工作台（docs/06）

---

## 1. 变更总览

| # | 变更点 | 类型 | 影响模块 | 优先级 |
|---|--------|------|---------|:--:|
| C-01 | 算力商品扩展为 4 种租赁范围类型 | 新增 | 算力撮合 | P0 |
| C-02 | 发布商品支持 4 种类型差异化表单 | 修改 | 算力撮合 | P0 |
| C-03 | 供给方工作台增加算力中心/托管机房视图 | 修改 | 多端工作台 | P0 |
| C-04 | 计费模式扩展为 h/d/w/m/n（n=买断） | 修改 | 算力撮合 | P0 |
| C-05 | 算力中心资源同步与盘点（主动盘/被动报） | 新增 | 算力撮合 | P0 |
| C-06 | 交付后访问权限管理（平台生成 key/value） | 新增 | 算力撮合 | P0 |
| C-07 | 融资租赁增加中登网动产登记查询 | 新增 | 居间金融 | P1 |
| C-08 | 设备商品发布（一手/二手） | 新增 | 居间金融 | P0 |

---

## 2. C-01：算力商品 4 种租赁范围类型

原设计只有"按卡分时/包月租赁"一种形态。现扩展为 4 类：

| 类型 | 代码值 | 说明 | 计费 | 交付物 |
|------|--------|------|------|--------|
| **零租** | `card_rental` | 按卡租用，短期灵活 | h / d / w | 卡的使用权（限期） |
| **零售** | `outright` | 买断，**使用权永久** | n（一次性） | 直接交付机器使用权 + 使用信息，信息在平台侧持续维护更新 |
| **成熟算力中心** | `center` | 整体打包（x 卡 / x 台 / 约 xxxP 算力） | d / w / m / n | 整柜或整机房交付 |
| **空心机房** | `colocation` | 已有机房但无设备，寻求设备入驻 | 议价 | 机柜位 + 电力 + 制冷 |

**关键字段扩展**：
```
product_type          零租/零售/算力中心/空心机房
machine_count         台数（算力中心用）
total_pflops_approx   总算力约 xxxP（算力中心用，纯文本，不做换算）
power_capacity        电力容量 MW（空心机房用）
```

> 注：`total_pflops_approx` 为供给方自行填报的**参考值**（如"约 128P"），平台**不做算力换算与校验**（已在需求评审中确认不做 GPU 算力换算表）。

---

## 3. C-04：计费模式扩展

| 代码值 | 含义 | 适用类型 |
|--------|------|----------|
| `hourly` | 按小时（h） | 零租 |
| `daily` | 按天（d） | 零租、算力中心 |
| `weekly` | 按周（w） | 零租、算力中心 |
| `monthly` | 按月（m） | 算力中心 |
| `perpetual` | 买断永久（n） | 零售 |

---

## 4. C-05：算力中心资源同步与盘点

**业务背景**：算力中心的实际可售余量会因自用、其他渠道售出而变化，平台需要掌握真实余量。

**两种同步方式**：

| 方式 | 触发方 | 说明 |
|------|--------|------|
| **主动盘** | 平台 | 平台定期发起盘点请求，机房确认当前余量 |
| **被动报** | 机房 | 机房主动上报余量变化 |

**数据结构**：`resource_snapshots` 表记录每次盘点的 `stock_before` / `stock_after` / `sync_type` / `operator_id`，形成余量变更审计链。

**异常处理**：盘点结果与平台记录差异超过阈值 → 标记 `anomaly` → 触发风控告警。

**接口**：
```
GET  /supplier/resource-syncs           盘点记录列表
POST /supplier/resource-syncs/active    发起主动盘点
POST /supplier/resource-syncs/passive   机房被动上报
```

---

## 5. C-06：交付后访问权限管理

**业务背景**：算力中心开放机器的特定权限给买家，需要平台统一管理"谁能访问什么"。

**方案**：平台在交付环节生成一对访问凭证（`access_key` / `access_value`），买家持此凭证向算力中心发起请求，算力中心校验后放行。

```
供给方回填交付信息
      ↓
平台生成 access_key (ak-xxxx) + access_value (加密存储)
      ↓
买家确认签收 → 获得凭证
      ↓
买家持凭证请求算力中心 → 算力中心校验 → 放行特定权限
```

**凭证状态**：`待生成` → `已生成` → `已交付` → `已失效`（订单结束/退款后自动失效）

**安全要求**：
- `access_value` 数据库中 AES-256 加密存储（待接入 KMS）
- 前端脱敏展示，点击"查看完整"需二次确认并记审计日志
- 订单到期/退款/冻结 → 凭证自动失效

> ⚠️ 待确认（Q-24）：算力中心侧如何校验凭证？需要平台提供校验 API，还是采用标准协议（如 OIDC）？涉及与各机房的对接协议，需逐一沟通。

---

## 6. C-07：融资租赁增加中登网动产登记

**业务背景**：融资租赁业务中，租赁物需在**中国人民银行征信中心动产融资统一登记公示系统（中登网）**登记，查询登记状态是尽调必要环节。

**v1 实现范围**：
- 提供登记信息**查询与展示**（按企业名称/统一社会信用代码）
- 展示字段：登记编号、登记类型、登记期限、标的物描述、状态
- **数据来源**：v1 为人工录入/线下同步；后续可评估中登网数据接口对接

**合规声明**：页面需注明"登记数据来源于中国人民银行征信中心动产融资统一登记公示系统，仅供参考，以官方系统查询结果为准"。

> ⚠️ 待确认（Q-25）：中登网是否提供对外数据接口？还是只能人工查询后录入？影响数据更新方式。

---

## 7. C-08：设备商品发布（一手/二手）

**业务背景**：原设计中设备销售仅有"居间线索"形态，缺少设备商品的挂牌展示。

**新增能力**：设备厂商可发布设备商品，买家可浏览检索。

| 字段 | 说明 |
|------|------|
| `equipment_type` | GPU服务器 / 存储 / 网络 / 制冷 / UPS / 机柜 / 其他 |
| `condition` | **一手（new）/ 二手（used）** |
| `quantity` | 数量 |
| `unit_price` | 单价（分） |
| `region` | 所在地域 |

**与算力商品的关系**：算力市场页面用 tab 区分「算力商品」和「设备商品」，共用检索框架但字段不同。

**交易模式**：v1 设备商品为**询价撮合**（线索转化），不做在线支付下单——设备金额大、需线下验货议价。

---

## 8. 对现有文档的影响

| 文档 | 需要更新的章节 |
|------|--------------|
| docs/03-功能需求-算力撮合交易 | REQ-A-010 商品字段、REQ-A-020 检索筛选、新增资源盘点与凭证管理需求 |
| docs/06-功能需求-多端账户与工作台 | REQ-D-031 供给方商品管理、新增资源盘点入口 |
| docs/07-功能需求-居间与金融服务板块 | REQ-E-006 设备库升级为设备商品、新增中登网登记查询 |
| docs/15-需求任务拆解 | 新增 T-068~T-075 共 8 个任务 |
| docs/17-API接口设计 | 新增资源盘点、设备商品、动产登记相关接口 |

---

## 9. 新增开放问题

| ID | 问题 | 影响 |
|----|------|------|
| Q-24 | 算力中心侧如何校验平台下发的访问凭证？需要什么对接协议？ | C-06 能否落地 |
| Q-25 | 中登网是否有数据接口？还是人工录入？ | C-07 数据更新方式 |
| Q-26 | 空心机房（colocation）的撮合流程是什么？是否走线上下单还是纯线索？ | C-01 交易闭环 |
| Q-27 | 零售（买断）交付后，"使用信息在平台维护更新"具体维护哪些信息？ | C-01 数据模型 |
| Q-28 | 设备商品是否需要在线支付？还是纯询价撮合？ | C-08 是否接支付 |

---

## 10. 实施记录（2026-07-30 完成）

### 10.1 需求落地位置

| 编号 | 后端实现 | 线稿实现 |
|------|---------|---------|
| C-01 | `compute/service.go` 商品类型常量 + `Product` 扩展 6 字段；`migrations/002` | page-market 四类型分段切换 + 差异化卡片 |
| C-02 | `compute/service.go` `ValidateProductReq()` 按类型强校验 | page-supplier-publish 类型选择 + 4 套差异化表单 |
| C-03 | `compute/service.go` `GroupProductsByType()`；`GET /supplier/products/summary` | page-supplier-dashboard 新增「成熟算力中心」「空心机房」板块 |
| C-04 | `pricing_mode` ENUM 扩至 5 值；`allowedPricingModes` 按类型限制 | 各类型筛选栏 + 发布表单计费下拉按类型联动 |
| C-05 | `compute/service.go` `SyncResource()` + `ComputeAnomaly()`；`resource_snapshots` 表 | page-supplier-dashboard 新增「资源盘点」板块 + 2 个弹框 |
| C-06 | `compute/service.go` `DeliverWithAccess()` / `RevealAccessCredential()` / `RevokeExpiredAccess()`；`pkg/crypto/aes.go` | page-buyer-order 新增「访问凭证」区 + 查看完整弹框 |
| C-07 | `intermediary/collateral.go`（model/repo/service/handler 单文件） | page-finance-page 新增中登网查询区 + 合规声明 |
| C-08 | `internal/equipment/` 新包（repository/service/handler） | page-equipment 升级为设备市场（一手/二手）+ 发布/询价弹框 |

### 10.2 数据库迁移

| 文件 | 内容 |
|------|------|
| `002_compute_market_expansion` | products 加 6 字段、pricing_mode 扩 ENUM、gpu_model/card_count/delivery_mode 改可空、新建 `resource_snapshots`、order_deliveries 加 5 个凭证字段 |
| `003_equipment_and_collateral` | 新建 `equipment_products`、`equipment_inquiries`、`collateral_registrations` |
| `004_fix_user_role_enum` | **修复 001 既存缺陷**（见 10.4） |

迁移已在真实 MySQL 8.0 容器验证：全新库 001→004 全部 apply 成功；down 回滚干净（products 恢复原 ENUM）；再次 up 可重入。

### 10.3 新增接口（21 条，全站共 82 条）

```
# C-03 / C-05 算力撮合
GET    /api/v1/supplier/products/summary               按类型分组统计
GET    /api/v1/supplier/resource-syncs                 盘点记录
POST   /api/v1/supplier/resource-syncs/passive         机房被动上报
POST   /api/v1/admin/resource-syncs/active             平台主动盘点

# C-06 访问凭证
GET    /api/v1/orders/:id/access-credential            脱敏返回
POST   /api/v1/orders/:id/access-credential/reveal     查看完整（记审计日志）

# C-08 设备商品
GET    /api/v1/equipments                              列表（类型/成色/地域/价格筛选）
GET    /api/v1/equipments/:id                          详情
POST   /api/v1/equipments/:id/inquiries                提交询价
GET    /api/v1/equipments/inquiries                    我的询价
GET    /api/v1/vendor/equipments                       厂商商品列表
POST   /api/v1/vendor/equipments                       发布
PATCH  /api/v1/vendor/equipments/:id/offline           下架
GET    /api/v1/vendor/equipments/inquiries             收到的询价
GET    /api/v1/admin/equipments                        运营列表
POST   /api/v1/admin/equipments/:id/approve            审核通过
POST   /api/v1/admin/equipments/:id/reject             审核驳回

# C-07 中登网动产登记
GET    /api/v1/collateral-registrations                按承租人查询（禁止空条件拉全表）
POST   /api/v1/admin/collateral-registrations          录入
PUT    /api/v1/admin/collateral-registrations/:id      修改
DELETE /api/v1/admin/collateral-registrations/:id      作废（软删）
```

### 10.4 本轮顺带修复的既存缺陷

实施过程中发现 5 个原有缺陷，均已修复：

| 严重度 | 缺陷 | 位置 | 后果 |
|:--:|------|------|------|
| **P0** | `PlaceOrder` 完全不校验 `Quantity` / `Duration` | `compute/service.go` | 传负数量 → 总价为负，且 `stock = stock - ?` 配 `stock >= ?` 判断会**凭空增加库存并通过校验**；传 `duration=0` → 总价 0 白嫖；超大值 → int64 溢出 |
| **P0** | `user_roles.role` ENUM 缺 `operator` / `admin` | `migrations/001:54` | 001 末尾种子语句整条中止（ERROR 1265 Data truncated），`user_roles` 表为空 → 种子管理员**没有任何角色**，`RBAC("operator","admin")` 保护的全部运营后台路由在全新部署上不可达 |
| P1 | `RenewOrder` 复用下单校验，会重复校验库存 | `compute/service.go` | 卡卖光后老客户**无法续租**（续租延长的是已持有的同一批卡，不应占用新库存）。已拆出 `ValidateRenewParams` |
| P2 | `switchMarketTab()` 被调用但从未定义 | `wireframe.html:208` | 点击导航「设备商品」抛 JS 错误 |
| P2 | 3 个弹框被 `openModal()` 引用但不在 `#modalOverlay` 内 | `wireframe.html` | `m-equip-inquiry` / `m-equip-order` / `m-finance-consult` 点击无反应——它们只定义在静态展示区，而 `openModal` 只在 overlay 内查找 |

> 另：按 C-08「v1 设备为询价撮合、不做在线支付」，已移除设备卡片的「立即采购」按钮与对应 `m-equip-order` 绑定。

---

## 11. 市场前端交付约束（2026-08-13）

- Figma 文件节点 `413:4622` 只作为布局、色彩、间距与资产规格，不作为组件源码。
- `/market` 的导航、按钮、卡片、选择器、输入、筛选标签、分页与空状态统一使用 HeroUI v3 / HeroUI Pro 组件；禁止退回浏览器原生选择弹层。
- Landing 与市场直接渲染同一个 `src/components/layout/public-header.tsx`：玻璃容器、定位、滚动显隐、导航链接、移动菜单与账户入口保持单一实现，不再存在页面级的第二套 Nav 组合。
- 全站路由跳转统一提供 HeroUI 加载反馈与轻量淡入过渡；根级 `loading.tsx` 兜底全部页面，业务分区只覆写加载文案，并遵守 `prefers-reduced-motion`。
- 公共导航与工作台侧栏保留在页面过渡之外，只切换内容区；市场查询在等待期间保留旧列表并提供轻量状态反馈，数据返回后再进入新内容，避免空白硬切。
- 市场氛围背景由市场 Layout 统一承载，从视口顶部连续覆盖导航与内容区，不再在透明导航下形成背景接缝。
- 市场筛选区的主筛选与高级筛选控件统一为 44px，高级筛选在宽屏保持单行；排序按钮保留 36px 次级层级，导航与页面标题使用独立的 32px 以上间距。
- 市场高级筛选仅保留搜索、交付方式、可售时段与最少卡数；“每页条数”移入分页控制组，卡数输入使用 HeroUI NumberField 完整步进结构。
- 列表 CTA 在 Phase 09 下单链路完成前保持为「查看详情」，不展示不可兑现的「立即下单」。信用数据缺失时显示「暂无评分」，不生成模拟分数。
- 已联调真实 Go API，并完成桌面、移动端、价格 URL、四类资源切换、详情入口和移动菜单验收。API 当前两条种子商品的 `cpu_spec` 存在 `2Ã—` 乱码，属于服务端种子数据清理项，前端不猜测改写业务数据。

### 10.5 质量验证

| 项 | 结果 |
|---|---|
| `go build ./...` | 通过 |
| `go vet ./...` | 无告警 |
| `go test ./...` | 全绿 |
| compute + crypto 新增测试 | 20 个顶层用例 / 105 个子用例，全部通过 |
| 路由注册 | 新增 `internal/routecheck/routes_test.go`，按 main.go 相同方式装配，82 条路由**无冲突无重复**（路由冲突只在注册时 panic，`go build` 查不出来） |
| 迁移 | 真实 MySQL 8.0 容器 up / down / up 循环验证通过 |
| 生产代码 mock 扫描 | 生产代码中**无** mock / fake / dummy / stub 残留 |
| 线稿结构 | HTML 标签配对 OK；2 个 script 块 `node --check` 通过；15 个页面 `navigate()` 目标全部存在；分段 tab 与 pane 一一对应；弹框引用全部可解析 |

### 10.6 仍需补充的外部依赖（未接入，代码返回明确错误而非假装成功）

| 依赖 | 现状 | 需要补充 |
|------|------|---------|
| 凭证加密密钥（C-06 前置） | `config.yaml` `security.credential_key` 为空 → 生成凭证时返回 `crypto.ErrKeyNotConfigured`；启动时打 WARN 日志 | 64 位 hex（32 字节）密钥，生产应从 KMS 注入 |
| 易宝支付 | `payment/repository.go` 返回 `ErrYeepayNotConfigured` | 商户号 + RSA2048 私钥 + API 地址 + 分账规则编号 |
| 短信验证码 | `auth/service.go` 注册流程返回明确错误 | 服务商 AccessKey + 签名 + 模板 ID |
| BSN-DDC 区块链 | `blockchain/repository.go` 返回 `ErrBSNNotConfigured` | BSN API Key + 存证合约地址 + 用户网关地址 |
| 中登网数据 | v1 设计为运营人工录入（`data_source` 仅 `manual`） | 中登网**无对外数据接口**，人工录入为最终形态而非待对接 |

### 10.7 开放问题在本轮的处理口径

| ID | 本轮采取的口径 | 是否仍需业务确认 |
|----|--------------|:--:|
| Q-24 | 平台生成 key/value 并加密存储、到期/退款自动吊销；**算力中心侧校验协议未定** | 是 |
| Q-25 | 按「中登网无对外接口」处理，仅支持运营人工录入 + 录入依据留痕 | 否 |
| Q-26 | 空心机房**不进在线下单**，`ValidateOrderParams` 直接拒绝，只走询价 | 是（若要线上闭环需改） |
| Q-27 | 交付说明字段承载「机器使用权 + 使用信息」，具体维护字段清单待定 | 是 |
| Q-28 | 设备商品**不接在线支付**，走询价撮合 | 是（已按此实现） |

---

> **状态**：C-01~C-08 全部实施完成，已通过构建 / 测试 / 迁移 / 结构校验 · **CR 审查报告见 `docs/19-CR审查报告v1.1.md`**

---

## 12. 账户入口前端约束（2026-08-17）

- 登录与注册页参考 Figma 节点 `413:5180` 的左右分栏、玻璃表单、尺寸和氛围资产，组件实现继续使用 HeroUI v3 / HeroUI Pro。
- 当前优先开放手机号验证码、邮箱验证码和微信扫码入口；账号密码与企业微信延后，不因 Figma 示例中的密码表单提前开放。
- `/auth/login`、`/auth/register` 及现有 `/auth/*` 页面从临时 Landing 路由封锁中放开。
- 本阶段不新增受保护页面的自动登录拦截；登录与注册页面本身也不因已有会话自动跳转。
- 手机与邮箱验证码目前由前端 Mock 适配层提供可测试闭环；短信、邮件和微信服务端渠道仍属于外部依赖，未标记为真实联调完成。
- 验证码使用单行输入框并内置“获取验证码”操作；发送前使用 Cap programmatic mode 完成人机验证，用户仅看到按钮内的验证状态，不再展示自制滑块或安全验证弹窗。
- 前端已接入 `cap-widget` programmatic mode；未配置 `NEXT_PUBLIC_CAP_API_ENDPOINT` 时直接拒绝发送，不再静默降级为演示 token。
- 本地使用 Cap Standalone + Valkey，公开站点端点为 `http://127.0.0.1:3210/local-dev/`；前端求解后通过同源 Route Handler 调用 Go `POST /api/v1/auth/captcha/verify` 完成 Siteverify，一次性 token 重放会被拒绝。
- 手机号流程已切换本地 Go API：Cap token 不再预先调用 `/auth/captcha/verify`，而是原样提交给 `POST /api/v1/auth/sms/code` 并在同一请求内一次性校验；前端同时接入 `POST /auth/sms/login` 与 `POST /auth/register`，所有响应均检查 JSON `code`，不以 HTTP 200 作为业务成功。
- 短信登录 token 由 Next Route Handler 写入 HttpOnly Cookie，不进入 localStorage；手机号注册按后端契约增加密码字段，注册成功后返回登录页，不伪装成已登录。
- 当前本地环境只验证接口契约、Cap token 传递与错误处理；真实阿里云短信发送验收待服务端部署完成后切换到服务器环境执行。邮箱验证码暂保留前端 Mock。
- 2026-08-18 前端候选镜像 `4a36a75` 已部署到 `omnisline.com`，生产 Cap `challenge → redeem`、市场 API 和认证 BFF 均已通过公网验收。线上后端 `b694aa9` 尚未包含 Cap Siteverify，无效 `captcha_token` 仍会被短信接口接受；真实短信投递、短信码注册和登录待使用可接收的测试手机号完成，本阶段不得标记为安全闭环。
- 登录和注册表单不展示浏览器原生校验文案；手机号、邮箱、账户名称和验证码统一使用明确的业务中文提示。
- 登录和注册方式切换使用 HeroUI Pro `Segment`，几何尺寸按 Figma 节点 `413:5180` 执行：外框高 `40px` / 圆角 `13px`，选中项高 `34px` / 圆角 `10px`，不使用组件默认的胶囊圆角。

---

## 13. 注册登录闭环调整（2026-08-20）

- 手机号注册只要求手机号验证码与用户主动勾选服务条款、隐私规则；当前阶段不采集登录密码，账号密码与企业微信继续按 P1 后置。
- 注册与短信登录都先验证一次性短信码，再读取账号是否存在及状态；仅提交任意验证码时，已注册与未注册手机号返回相同错误，不暴露账号存在性。
- 手机号注册成功后由同一次服务端响应签发会话，不再要求用户返回登录页重新获取验证码；新账户默认获得采购方身份，不强制跳转实名认证。
- 登录与注册之间切换时保留经过同源校验的 `next` 目标；成功后只回到当前账号角色可访问的已知路由，无权、未知或认证入口目标回退到默认身份工作台。
- 认证 token 只保存在 HttpOnly Cookie；前端通过同源 `/api/auth/me` 恢复账户状态，access token 失效时使用 refresh token 轮换，退出登录同时调用服务端注销并清除全部认证 Cookie。
- 会话恢复不信任 localStorage 中的账户 ID、角色或旧 Mock 快照；所有账户状态均通过 HttpOnly Cookie 调用 `/api/auth/me` 重新确认，旧演示管理员数据不能恢复成生产会话。
- refresh token 采用一次性轮换：每个 JWT 含加密随机唯一 `jti`，成功刷新后旧 token 立即进入 Redis 撤销集，同一秒签发的新旧 token 也不会相同；退出时 access/refresh token 同时撤销。
- refresh 时重新读取数据库中的账号状态与角色：已删除或已冻结账号不得续期，新 token 使用当前权限而不是旧 token 中的过期权限。
- refresh 轮换成功后立即保存新 HttpOnly Cookie；即使紧接着的 `/auth/me` 资料读取短暂失败，也不会丢弃新 token 并陷入无法自愈的退出状态。
- 浏览器不对外暴露单独 refresh Route Handler；会话刷新只由同源 `/api/auth/me` 在服务端完成，新 access/refresh token 仅写入 HttpOnly Cookie，不出现在响应 JSON 中。
- `/auth/logout` 依据传入 token 的服务端签名完成幂等撤销；即使 access cookie 已过期或不存在，BFF 仍会传入 refresh cookie 使其服务端失效，不只做本地清理。
- 受保护请求先验证 JWT 签名，再查询 Redis 撤销状态；有效 token 的撤销状态无法确认时返回 503，不在 Redis 故障时放行可能已退出的会话。
- `/auth/me` 恢复会话时返回与登录、注册相同的脱敏手机号，不再用 JWT claim 中的原始手机号覆盖。
- 账号密码仍属 P1 后置：服务端不再注册公开 `POST /auth/login` 路由；新建数据库不再写入固定 `admin/admin123` 管理员，迁移 006 仅删除密码哈希仍与早期种子一致的默认账号。管理员必须通过受控运维流程显式创建。
- 发布链路持续维护上述约束：公网 Caddy 对精确路径 `POST /api/v1/auth/login` 直接返回 404，后端发布脚本在候选验证与切流后都会拒绝仍暴露该公开入口的镜像。
- Go 服务端 `/auth/me` 与 `/auth/logout` 必须注册在鉴权中间件之后；公开路由只保留验证码发送、注册、登录与 refresh。
- 邮箱验证码和微信扫码入口保留为产品优先项，但在真实邮件服务与微信开放平台接口完成前不得标记为生产闭环，也不得用前端 Mock 冒充真实投递或授权。
- 邮箱与微信入口在真实渠道接入前使用 HeroUI Pro 禁用态明确标注“待开放”；生产表单不再调用邮箱登录、邮箱注册的浏览器 Mock hook。
- 登录按钮只在手机号与 6 位验证码均有效时可用；注册按钮还必须同时完成条款勾选，避免空表单进入错误请求。
- 短信接口分别返回验证码有效期 `expires_in=300` 与重发冷却 `resend_after=60`；登录、注册共用的验证码控件仅使用后者倒计时，不再误锁 5 分钟。
- 短信 BFF 只向 Go 服务转发由入口 Caddy 生成的 `X-Forwarded-For`，使手机号与客户端 IP 双维限流按真实访客生效，不再把所有网页用户合并为前端容器 IP。
